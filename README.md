# Backtest Lab — Web

UI for the [backtest-lab-api](../backtest-lab-api). Pick a strategy, configure costs and slippage, run a single backtest with regime decomposition or a walk-forward stability test.

## Setup
```bash
npm install
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```
